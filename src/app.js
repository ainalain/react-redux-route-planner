import React from 'react';
import { render } from 'react-dom';
import { Provider } from 'react-redux';
import styles from './layout.scss';

render(<div>React route planner</div>, document.getElementById('app'));
